package com.chat.talkMe.enums;

/**
 * Languages a user speaks (multi-select). Used by the compatibility engine
 * (shared-language factor) and matching filters (language filter, feature #3).
 */
public enum Language {
    EN, ES, FR, DE, IT, PT, NL, PL, RU, TR,
    AR, HI, UR, BN, PA, TA, TE, MR, GU,
    ZH, JA, KO, VI, TH, ID, MS, FIL,
    FA, SW, HE, EL,
    OTHER
}
