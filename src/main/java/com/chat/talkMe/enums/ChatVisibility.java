package com.chat.talkMe.enums;

/**
 * PRIVATE — invite/link only, not surfaced in discovery.
 * PUBLIC  — discoverable (searchable, may have a @slug handle).
 */
public enum ChatVisibility {
    PRIVATE,
    PUBLIC
}
