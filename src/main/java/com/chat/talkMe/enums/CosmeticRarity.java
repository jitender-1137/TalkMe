package com.chat.talkMe.enums;

/**
 * Visual rarity tier of a cosmetic (Phase 4 gamification surface). Decoration only — used to
 * style the catalog card; carries no functional weight.
 */
public enum CosmeticRarity {
    COMMON,
    RARE,
    EPIC,
    LEGENDARY
}
